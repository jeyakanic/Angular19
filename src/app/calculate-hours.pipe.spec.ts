import { CalculateHoursPipe } from './calculate-hours.pipe';

describe('CalculateHoursPipe', () => {
  it('create an instance', () => {
    const pipe = new CalculateHoursPipe();
    expect(pipe).toBeTruthy();
  });
});
