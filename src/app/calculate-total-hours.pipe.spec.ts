import { CalculateTotalHoursPipe } from './calculate-total-hours.pipe';

describe('CalculateTotalHoursPipe', () => {
  it('create an instance', () => {
    const pipe = new CalculateTotalHoursPipe();
    expect(pipe).toBeTruthy();
  });
});
